% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_celldeath_multigroup.R
\name{plot_death_compare_bubble}
\alias{plot_death_compare_bubble}
\title{Visualization of Multi-Group Cell Death Enrichment Comparison}
\usage{
plot_death_compare_bubble(
  compare_res,
  show_category = 10,
  palette = c("#2E8B57", "#F39C12", "#E74C3C"),
  title = "Cell Death Pathway Enrichment (Multi-Group)",
  filename = NULL,
  return_data = FALSE
)
}
\arguments{
\item{compare_res}{A \code{compareClusterResult} object returned by \code{celldeath_compare_enrich()}.}

\item{show_category}{Number of top pathways to display per group (sorted by p-value).}

\item{palette}{Color palette for p.adjust (low to high). Default: c("#2E8B57", "#F39C12", "#E74C3C").}

\item{title}{Plot title. Default: "Cell Death Pathway Enrichment (Multi-Group)".}

\item{filename}{If provided, save plot to file (e.g., "compare_bubble.png"). Default: NULL.}

\item{return_data}{If TRUE, return the data frame used for plotting. Default: FALSE.}
}
\value{
A \code{ggplot2} object (or data frame if return_data = TRUE).
}
\description{
Generate a dotplot for multi-group cell death pathway enrichment comparison.
Support two analysis modes from celldeath_compare_enrich():
\enumerate{
\item Global mode: all groups compare against all cell death pathways
\item Targeted mode: all groups compare against one single specified pathway
}
}
\examples{
data("demo_deg_list", package = "CDEnrich")
data("demo_all_genes", package = "CDEnrich")
data("representative_genes", package = "CDEnrich")

compare_result <- celldeath_compare_enrich(
  deg_list_list = demo_deg_list,
  universe = demo_all_genes
)

plot_death_compare_bubble(
  compare_result,
  show_category = 10
)
}
