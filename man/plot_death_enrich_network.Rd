% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_celldeath_singlegroup.R
\name{plot_death_enrich_network}
\alias{plot_death_enrich_network}
\title{Visualization of Cell Death Enrichment Results (Enrichment Network)}
\usage{
plot_death_enrich_network(
  enrich_res,
  show_category = 30,
  color = "p.adjust",
  filename = NULL,
  ...
)
}
\arguments{
\item{enrich_res}{An \code{enrichResult} object returned by \code{celldeath_enrich()}.}

\item{show_category}{Number of top pathways to display (sorted by p-value). Default: 30.}

\item{color}{Which variable to use for node color. Options: "p.adjust" (default), "pvalue", "qvalue", "Count", etc.}

\item{filename}{If provided, save plot to file (e.g., "enrich_network.png"). Default: NULL.}

\item{...}{Additional arguments passed to \code{enrichplot::emapplot} (e.g., node_label, cex_label_group).}
}
\value{
A \code{ggplot2} object.
}
\description{
Generate an enrichment network plot for single-group cell death pathway enrichment analysis.
Automatically adapt two analysis modes from celldeath_enrich():
\enumerate{
\item Global mode: multiple pathways, full gene overlap network with edges
\item Targeted mode: single specified pathway, simple single-node plot
Nodes represent pathways, edges represent gene overlaps (similarity)(only for multi-pathway).
}
}
\examples{
data("demo_deg", package = "CDEnrich")
data("demo_all_genes", package = "CDEnrich")
data("representative_genes", package = "CDEnrich")

ora_result <- celldeath_enrich(
  deg = demo_deg,
  universe = demo_all_genes
)

plot_death_enrich_network(
  ora_result,
  show_category = 10
)
}
