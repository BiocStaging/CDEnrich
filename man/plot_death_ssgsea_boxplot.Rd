% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_celldeath_ssGSEA.R
\name{plot_death_ssgsea_boxplot}
\alias{plot_death_ssgsea_boxplot}
\title{Visualization of Cell Death ssGSEA Scores (Grouped Boxplot)}
\usage{
plot_death_ssgsea_boxplot(
  ssgsea_result,
  show_category = 10,
  palette = c("#2E8B57", "#F39C12", "#E74C3C", "#8E44AD"),
  add_points = TRUE,
  title = "Cell Death Pathway ssGSEA Score Comparison",
  filename = NULL,
  return_data = FALSE
)
}
\arguments{
\item{ssgsea_result}{A \code{celldeath_ssgsea} object returned by
\code{celldeath_ssgsea()} (must have non-NULL \code{group}).}

\item{show_category}{Number of top pathways to display (ordered by mean
absolute score). Default: 10.}

\item{palette}{Colors for groups. Default: c("#2E8B57", "#F39C12", "#E74C3C", "#8E44AD").}

\item{add_points}{Logical. Whether to overlay jittered sample points. Default: TRUE.}

\item{title}{Plot title. Default: "Cell Death Pathway ssGSEA Score Comparison".}

\item{filename}{If provided, save plot to file (e.g., "boxplot.png"). Default: NULL.}

\item{return_data}{If TRUE, return the data frame used for plotting. Default: FALSE.}
}
\value{
A \code{ggplot2} object (or data frame if return_data = TRUE).
}
\description{
Generate boxplots comparing ssGSEA score distributions across groups.
Best companion for \code{celldeath_ssgsea_diff()} results.
Automatically adapt for two modes:
\enumerate{
\item Global mode: pathways on x-axis (faceted or dodged by group)
\item Targeted mode: single-pathway group comparison layout
}
}
\examples{
data("demo_expr", package = "CDEnrich")
data("demo_group", package = "CDEnrich")
data("representative_genes", package = "CDEnrich")

ssgsea_result <- celldeath_ssgsea(
  expr = demo_expr,
  group = demo_group
)

plot_death_ssgsea_boxplot(
  ssgsea_result,
  show_category = 10
)
}
