% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_celldeath_singlegroup.R
\name{plot_death_gsea_ranked}
\alias{plot_death_gsea_ranked}
\title{Plot GSEA Ranking Score Curve for Cell Death Pathways}
\usage{
plot_death_gsea_ranked(
  gsea_res,
  pathway,
  use_recommended = TRUE,
  color = "#2E8B57",
  base_size = 10,
  title = NULL,
  filename = NULL,
  ...
)
}
\arguments{
\item{gsea_res}{Result from \code{celldeath_gsea()}.}

\item{pathway}{Character. Either an \strong{exact pathway ID} present in the
result, or a \strong{cell death type name} (e.g. "Ferroptosis"), which is
resolved to the recommended representative gene set when available
(see \code{\link{get_representative_pathway}}).}

\item{use_recommended}{Logical. Only takes effect when \code{pathway} is
a cell death type with a curated recommendation: TRUE (default) plots
the recommended representative gene set; FALSE plots ALL gene sets of
this type present in the result.}

\item{color}{Color of the running score line. Default: "#2E8B57".}

\item{base_size}{Base font size. Default: 10.}

\item{title}{Plot title. If NULL, uses the pathway name. Default: NULL.}

\item{filename}{If provided, save plot to file (e.g., "gsea_ranked.png"). Default: NULL.}

\item{...}{Additional arguments passed to \code{enrichplot::gseaplot}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Generate a running score plot (ranked list metric) for a single GSEA result.
Compatible with two modes from celldeath_gsea():
\enumerate{
\item Global mode: ranked list vs all cell death pathways
\item Targeted mode: ranked list vs one specific single pathway
This plot shows the enrichment score as a function of the ranked gene list.
}
}
