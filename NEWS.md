# CDEnrich 0.99.0

* Initial submission to Bioconductor.
* Curated cell death pathway gene sets: `load_cell_death_genes()`,
  `representative_genes`, `representative_pathways`.
* ORA: `celldeath_enrich()`, `celldeath_compare_enrich()`.
* GSEA: `celldeath_gsea()`, `celldeath_gsea_multiple()`.
* ssGSEA: `celldeath_ssgsea()` and plotting functions for
  single- and multi-group results.
